<!DOCTYPE html>
<html>
<head>
  <title>Blog</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-5">
    <a href="/add-post" class="btn btn-primary w-100">Создать заметку</a>
</div>

<div class="container mt-5">
    <h1>Latest Notes</h1>
    
    <% posts.forEach(post => { %>
      <div class="card mb-3">
        <div class="card-body">
          <h5 class="card-title"><%= post.title %></h5>
          <p class="card-text"><%= post.content %></p>
          <small class="text-muted">
            Автор: <strong><%= post.author || "Неизвестный" %></strong> | 
            Posted: <%= post.createdAt.toLocaleString('ru-RU', { timeZone: 'Europe/Moscow' }) %>
          </small>
        </div>
      </div>
    <% }); %>
    
</div>

</body>
</html>
